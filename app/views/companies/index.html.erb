<h1>Liste des entreprises</h1>

<table>
  <thead>
    <tr>
      <th>Nom</th>
      <th>SIRET</th>
      <th>Nombre d'émissions</th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %></td>
        <td><%= company.siret %></td>
        <td><%= company.emissions_count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
